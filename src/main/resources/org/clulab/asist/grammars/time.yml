vars: org/clulab/asist/grammars/vars.yml

rules:


  - name: time
    label: TimeUnit
    priority: 1
    type: token
    pattern: |
      (?<value> [tag=CD]+ (or [tag=CD]+)?) (?<unit> [lemma=/second|minute|hour|day|year/])
