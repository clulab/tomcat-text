vars: org/clulab/asist/grammars/vars.yml

rules:


  - name: ported_syntax_1_verb-${addlabel}
    priority: ${ rulepriority }
    example: ""
    label: ${ label }
    pattern: |
      trigger = [word=/(?i)^(${ trigger })/]
      theme: Concept = >dobj
      instrument: Concept? = >nmod_with
