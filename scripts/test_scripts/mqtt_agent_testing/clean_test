#!/bin/bash

# Author: Joseph Astier

# Remove files generated by testing
# This script is called by each test 

echo "cleaning $1 test ... "

for f in $2 $3 $4 $5
do
    if [ -s $f ]
    then
        echo "  deleting $f"
        rm $f
    fi
done

echo "done"
