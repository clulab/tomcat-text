#!/bin/bash

# Author: Joseph Astier

# list files generated by testing

test_name=$1
echo "$test_name test result files:"

for file in $2 $3 $4 $5
do
    if [ -f "$file" ]
    then
        echo -n "  "
	echo  $(wc -c $file)
    fi
done
