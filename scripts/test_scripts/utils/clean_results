#!/bin/bash

# Author: Joseph Astier

# Remove files generated by testing
# This script is called by each test 

test_name=$1
echo "cleaning $test_name test ... "

for file in $2 $3 $4 $5
do
    if [ -f $file ]
    then
        echo "  deleting $file"
        rm $file
    fi
done

echo "done"
